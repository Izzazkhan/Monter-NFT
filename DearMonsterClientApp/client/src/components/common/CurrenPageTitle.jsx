import React from 'react'

const CurrenPageTitle = ({ title }) => {
	return (
		<div>
			<section className='headingBox mt-6 bold fs-17 center mx-auto h-70px' style={{ width: "270px" }}>{title}</section>
		</div>

	);
}

export default CurrenPageTitle
